# 6.20:  Loop through the "outer" list (value_table) and print
# the sum of the values in each "inner" list (use the sum()
# function -- don't attempt to loop through the "inner"
# lists).  Print each sum rounded to 3 places as you loop.

import runreport

value_table =       [
                       [ 0.09, -0.22, -0.30, 0.009 ],
                       [ 0.44, -0.35, -0.08, 0.009 ],
                       [ 0.17, 0.26,  -0.37, 0.009 ]
                    ]

# Expected Output:

# -0.421
# 0.019
# 0.069

