# 8.17:  (running from command line only) Read from sys.argv.

# Running the below code from the command line, print the
# value of sys.argv.  Then, run the code again with the
# following 3 "words" on the same line when executing the
# script:  greetings filesystem arguments.

import sys

print(sys.argv)

# Expected Output:

# ['inclass_X.py', 'greetings', 'filesystem', 'arguments']

